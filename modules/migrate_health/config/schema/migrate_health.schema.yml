migrate_health.settings:
  type: config_object
  label: 'Migration health settings'
  mapping:
    stale_threshold:
      type: integer
      label: 'Stale threshold (days)'
    failure_rate_threshold:
      type: float
      label: 'Failure rate threshold (%)'
