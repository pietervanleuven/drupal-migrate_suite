services:
  migrate_source_field.provenance_lookup:
    class: Drupal\migrate_source_field\Service\ProvenanceLookup
    arguments: ['@database', '@plugin.manager.migration', '@migrate_suite.table_name_resolver', '@cache.default']

  Drupal\migrate_source_field\Hook\MigrateSourceFieldHooks:
    arguments: ['@entity_type.manager']
